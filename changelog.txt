+ Neverlose ClickGUI theme with module/category search, keybind editing, settings panels, and local config save/load/search support
+ HUD Neverlose style and draggable Hotkeys overlay with animated module entries
+ Insert key can open ClickGUI independently of its configured bind
+ BedESP module with configurable fill, outline, and color rendering for discovered beds
+ ZombieAim server-position resolver with latency-aware collision prediction, plus configurable bullet tracers
+ ThrowableAura targeting rewrite with friend/team/bot filtering, raycast validation, prediction, auto-switch restoration, and target marker
+ KeepSprint Vanilla and Prediction modes, including configurable slowdown, Ground Only, and On Hurt options
+ `hide` command now supports toggling all modules in a category
+ InventoryCleaner Instant mode now separates armor, hotbar, and drop actions across ticks
+ Eggs and snowballs are retained by InventoryCleaner
+ SkeetUI automatically scales and handles input correctly on small resolutions
+ TargetHUD now renders player skin heads
+ Improved Neverlose HUD and Hotkeys rendering with shader-based rounded shapes, bloom, and smoother animations
+ ESP2D box rendering consolidated for consistent Box, Split, and Corner styles
+ NoSlow Hypixel mode with an Only Aura option

- Fixed HUD rainbow animation regression
- Restored modules unintentionally omitted from ModuleManager registration
- Fixed KeepSprint and AntiKnockBack blink interaction so attack slowdown is configurable and blink does not force sprint loss

? NoSlow Watchdog mode renamed to Hypixel and now defaults to Hypixel
? KeepSprint defaults to Vanilla mode with 0% configured slowdown
? ThrowableAura now targets players only and pauses while Aura, Blink, Scaffold, or item use is active
+ BackTrack now displays delayed targets as translucent player models.
+ AntiKnockBack adds a ground-delay option for velocity handling.
+ BedESP fades out beds after they are removed.

- Scaffold removes the Hypixel SW and 45-degree rotation modes.

? Renamed the Disabler implementation to Bypass and changed its available mode.
? Aura reliably restores OpenGL state after rendering target indicators.
? AutoTool no longer switches tools while an item is in use.
? InventoryCleaner prioritizes larger throwable and block stacks.
? Session login returns to the screen that opened it.
? Font rendering preserves transparent glyph atlas backgrounds.
? DataWatcher ignores updates whose object type does not match the existing entry.